import React from 'react';
import ReactFilestack from 'filestack-react';
import Paper from 'material-ui/Paper';

const style = {
  height: 500,
  width: 500,
  margin: 20,
  textAlign: 'center',
  display: 'inline-block',
};

const Table = () => (
  <div>
  <h2>This is where the table goes</h2>
</div>
);

export default Table;
